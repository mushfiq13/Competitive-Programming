#include <bits/stdc++.h>
using namespace std;

const int MAX = 1000 + 10;

int main() {
    int t, n;

    scanf("%d", &t);
    while (t--) {
        scanf("%d", &n);
        for (int i=1; i<=n; ++i) {
            int x;
            scanf("%d", &x);
        }
        printf("%d\n", n-1);
    }

    return 0;
}
