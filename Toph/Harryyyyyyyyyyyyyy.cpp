#include <bits/stdc++.h>
using namespace std;

int main() {
    int t, n;

    scanf("%d", &t);
    for (int cs = 1; cs <= t; ++cs) {
        scanf("%d", &n);
        printf("Case %d: Harr", cs);
        while (n--)
            putchar('y');
        printf("! Khelbe Hogwarts, Jitbe Hogwarts!\n");
    }

    return 0;
}
