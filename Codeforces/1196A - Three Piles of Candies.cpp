#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    int q;

    cin >> q;
    while (q--) {
        ll a, b, c;
        cin >> a >> b >> c;
        cout << (a+b+c) / 2 << endl;
    }
}
